1. Add in every module in exchange of speak.

            if os.uname()[1] == 'dslave':
            speak(tts)
        else:
            speak('gnome-terminal is not working')

2. Check all newly added skills in Knee-jerk folder.

